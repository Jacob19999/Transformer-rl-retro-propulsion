[pytest]
cache_dir = simulation/test_cache
pythonpath = simulation/isaac
testpaths = simulation/isaac/tests/unit

markers =
    isaac: tests that require Isaac Sim / IsaacLab (GPU, pxr bindings). Skipped in CPU-only CI.

addopts = -m "not isaac"
