# Isaac Sim environment config — optimized training config (T038)
# Safe default for RTX 5070 (12 GB VRAM): 256 envs.
# Usage: python -m simulation.training.scripts.train_isaac_ppo --config simulation/isaac/configs/isaac_env_training.yaml

extends: isaac_env_base.yaml

num_envs: 256

